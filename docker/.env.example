# =============================================================================
# Docker Environment Variables for PostgreSQL & MySQL with SSL
# Private Blockchain - Development/Testing Environment
# =============================================================================
# Instructions:
#   1. Copy this file to .env: cp .env.example .env
#   2. Update passwords and settings as needed
#   3. DO NOT commit .env to version control
# =============================================================================

# =============================================================================
# PostgreSQL Configuration (Recommended for Production)
# =============================================================================

# PostgreSQL Database Name
POSTGRES_DB=blockchain_prod

# PostgreSQL User
POSTGRES_USER=blockchain_user

# PostgreSQL Password (CHANGE THIS FOR PRODUCTION!)
POSTGRES_PASSWORD=SecurePassword123!

# PostgreSQL Port (default: 5432)
POSTGRES_PORT=5432

# pgAdmin4 Configuration
PGADMIN_DEFAULT_EMAIL=admin@blockchain.local
PGADMIN_DEFAULT_PASSWORD=AdminPassword123!
PGADMIN_PORT=5050

# =============================================================================
# MySQL Configuration (Alternative)
# =============================================================================

# MySQL Root Password (CHANGE THIS FOR PRODUCTION!)
MYSQL_ROOT_PASSWORD=RootPassword123!

# MySQL Database Name
MYSQL_DATABASE=blockchain_prod

# MySQL User
MYSQL_USER=blockchain_user

# MySQL Password (CHANGE THIS FOR PRODUCTION!)
MYSQL_PASSWORD=SecurePassword123!

# MySQL Port (default: 3306)
MYSQL_PORT=3306

# phpMyAdmin Configuration
PHPMYADMIN_PORT=8080

# =============================================================================
# SSL/TLS Certificate Configuration
# =============================================================================

# Certificate Validity (days) - Default: 730 days (2 years, NIST-compliant)
CERT_VALID_DAYS=730

# RSA Key Size (bits) - Default: 3072 (NIST-compliant for post-2030 security)
RSA_KEY_SIZE=3072

# Certificate Country Code
CERT_COUNTRY=ES

# Certificate State/Province
CERT_STATE=Barcelona

# Certificate Locality/City
CERT_LOCALITY=Barcelona

# Certificate Organization
CERT_ORGANIZATION=PrivateBlockchain

# =============================================================================
# Docker Network & Volume Configuration
# =============================================================================

# Network Name
NETWORK_NAME=blockchain_network

# Volume Names
POSTGRES_VOLUME=blockchain_postgres_data
MYSQL_VOLUME=blockchain_mysql_data
PGADMIN_VOLUME=blockchain_pgadmin_data

# =============================================================================
# Java Application Configuration (for reference)
# =============================================================================
# Use these settings in DatabaseConfig class
# =============================================================================

# PostgreSQL JDBC URL:
# jdbc:postgresql://localhost:5432/blockchain_prod?ssl=true&sslmode=require

# MySQL JDBC URL:
# jdbc:mysql://localhost:3306/blockchain_prod?useSSL=true&requireSSL=true&trustServerCertificate=true&allowPublicKeyRetrieval=true
